{
  "name": "clog-keeper",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal permissionless keeper automation for the CLOG protocol - closes due rounds, qualifies matured tokens, retries failed randomness requests, and relays fulfilled VRF results from Arbitrum to Robinhood Chain. Never deployed/enabled without explicit operator approval - see README.md.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.test.json",
    "start": "node dist/index.js",
    "dry-run": "node dist/index.js --dry-run",
    "dev": "tsx src/index.ts",
    "dev:dry-run": "tsx src/index.ts --dry-run",
    "test": "vitest run"
  },
  "dependencies": {
    "viem": "^2.19.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.19.0",
    "typescript": "^5.5.0",
    "vitest": "^2.1.0"
  }
}
