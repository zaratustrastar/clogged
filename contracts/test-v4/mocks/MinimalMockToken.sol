// SPDX-License-Identifier: MIT
pragma solidity 0.8.26;

/// @notice Bare-minimum ERC20 for this test suite only - just enough surface (mint, transfer,
///         approve/transferFrom) to prove the real PoolManager deposit+claim sequence, not a
///         stand-in for the real MemeToken's own economics.
contract MinimalMockToken {
    string public name = "Mock CLOG Ticker";
    string public symbol = "MOCK";
    uint8 public decimals = 18;
    uint256 public totalSupply;
    mapping(address => uint256) public balanceOf;
    mapping(address => mapping(address => uint256)) public allowance;

    function mint(address to, uint256 amount) external {
        totalSupply += amount;
        balanceOf[to] += amount;
    }

    function transfer(address to, uint256 amount) external returns (bool) {
        balanceOf[msg.sender] -= amount;
        balanceOf[to] += amount;
        return true;
    }

    function approve(address spender, uint256 amount) external returns (bool) {
        allowance[msg.sender][spender] = amount;
        return true;
    }

    function transferFrom(address from, address to, uint256 amount) external returns (bool) {
        allowance[from][msg.sender] -= amount;
        balanceOf[from] -= amount;
        balanceOf[to] += amount;
        return true;
    }
}
